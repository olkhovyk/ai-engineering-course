apiVersion: v1
kind: Service
metadata:
  name: ollama-ui
  labels:
    app: ollama-ui
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ollama-ui
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
