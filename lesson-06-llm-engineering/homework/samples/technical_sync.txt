Наталя: Колеги, сьогодні синхронізуємось по імпорту ігрових даних у RAG-сервіс. Потрібно закрити ingestion pipeline, пошук і моніторинг.

Дмитро: По ingestion я бачу проблему: зараз chunking ріже markdown посеред таблиць. Я перепишу chunker так, щоб він зберігав заголовок секції і не ламав markdown-таблиці. Реалістичний дедлайн - 14 травня 2026.

Олена: Для Qdrant треба додати metadata поля: game_id, source_url, document_type і updated_at. Я можу оновити схему payload і міграційний скрипт до 13 травня 2026.

Максим: На API-рівні треба endpoint для ручного reindex. Я пропоную POST /admin/reindex з параметрами game_id і source_dir. Я зроблю endpoint, перевірку параметрів і логування job_id до 15 травня 2026.

Наталя: Рішення: endpoint reindex закриваємо admin token, без публічного доступу. Також reindex має працювати асинхронно, щоб HTTP-запит не висів кілька хвилин.

Ірина: По frontend: рекрутеру треба бачити, з яких джерел прийшла відповідь. Я додам у chat UI блок citations з title, source_url і score. Дедлайн - 16 травня 2026.

Дмитро: Є ще питання з embedding model. Зараз bge-small працює швидко, але на складних запитах програє. Я б не міняв модель до демо.

Наталя: Так і фіксуємо рішення: для демо залишаємо поточну embedding model, а порівняння з більшою моделлю винесемо в окремий експеримент після 20 травня.

Олена: Для моніторингу потрібні latency і кількість знайдених chunks. Я додам structured logs для retrieval_latency_ms, chunks_count і top_score до 14 травня 2026.

Максим: Якщо reindex падає, треба бачити помилку в логах і не чистити стару колекцію до успішного завершення.

Наталя: Рішення: використовуємо безпечний rollback - нову колекцію створюємо окремо, перемикаємо alias тільки після успішного індексування.

Ірина: Я ще додам empty state для випадку, коли citations порожні, але це можна зробити разом із блоком citations.

Наталя: Підсумок: Дмитро відповідає за chunker, Олена за Qdrant metadata і retrieval logs, Максим за admin reindex endpoint, Ірина за citations у chat UI. Архітектурні рішення: admin token, async reindex, поточна embedding model для демо і rollback через alias.
